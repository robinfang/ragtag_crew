# Pi + Telegram Agent 方案

> 基于 Pi Agent SDK + Telegram Bot 的轻量 Agent 方案
> 创建时间：2026-04-03 | 更新时间：2026-04-03

---

## 1. 目标与定位

**开源项目**，用户自行部署到本地电脑，**单用户使用**。

不依赖 OpenClaw，用 Pi Agent SDK 作为 agent 引擎，grammy 作为 Telegram 接入层，搭建一个最小可用的 Telegram AI Agent。

**核心价值：**
- 代码量小（~500 行），易于理解和定制
- 依赖少，部署简单
- 完全可控，不受第三方平台约束

## 2. SDK 选型

### 2.1 基本信息

| 维度 | @mariozechner/pi-coding-agent | @oh-my-pi/pi-coding-agent |
|---|---|---|
| 版本 | 0.64.0 | 13.18.0 |
| 首次发布 | 2025-11-12 | 2026-01-02 |
| 最后更新 | 2026-03-29 | 2026-04-02 |
| 总发布次数 | 185 次 | ~270+ 次 |
| 作者 | Mario Zechner (Pi 原作者) | Can Boluk (fork) |
| 仓库 | github.com/badlogic/pi-mono | github.com/can1357/oh-my-pi |
| License | MIT | MIT |
| CLI 命令 | `pi` | `omp` |

### 2.2 运行时要求

| 维度 | mariozechner | oh-my-pi |
|---|---|---|
| Node.js | >= 20.6.0 ✅ | 不要求 |
| Bun | 不要求 | **>= 1.3.7（必须）** |
| 导出格式 | 编译后的 `.js` + `.d.ts` | **原始 `.ts` 源文件** |

**关键差异：** oh-my-pi 所有包直接导出 `.ts` 源码，强制要求 Bun 运行时。mariozechner 导出编译后的 JS，标准 Node.js 20+ 即可运行。

**对本项目的影响：** 选 oh-my-pi 意味着运行时从 Node.js 切换到 Bun，进程管理、兼容性等都需要基于 Bun 考虑。

### 2.3 包体大小

| 包 | mariozechner | oh-my-pi |
|---|---|---|
| pi-coding-agent | 645 文件 / 9.8 MB | 860 文件 / 7.16 MB |
| pi-ai | 174 文件 / 3.78 MB | 128 文件 / 2.85 MB |
| pi-agent-core | 22 文件 / 244 KB | 9 文件 / 92 KB |
| pi-tui | 102 文件 / 1.5 MB | 31 文件 / 369 KB |
| **pi-natives** | **无** | **38 文件 / 75.4 MB** |

**注意：** oh-my-pi 的 `pi-natives` 是 Rust 原生模块（grep、clipboard、图片处理、PTY 等），75.4 MB。且当前版本停留在 **13.3.7**，其他包均为 13.18.0，**版本不同步**。

加上 puppeteer（需下载 ~200 MB Chromium），oh-my-pi 实际安装总大小可能超过 **300 MB**，而 mariozechner 约 **15 MB**。

### 2.4 依赖链

**mariozechner（4 个包）：**
```
pi-coding-agent (19 deps)
├── pi-agent-core (1 dep: pi-ai)
│   └── pi-ai (14 deps: anthropic, openai, google, bedrock, mistral, ajv, chalk, undici...)
├── pi-tui (4 deps: chalk, marked, mime-types, get-east-asian-width)
└── 其他: jiti, photon-node, ajv, cli-highlight, diff, extract-zip, file-type,
         glob, hosted-git-info, ignore, marked, minimatch, proper-lockfile,
         strip-ansi, undici, yaml
```

**oh-my-pi（6 个包）：**
```
pi-coding-agent (18 deps)
├── pi-agent-core (2 deps: pi-ai, pi-utils)
│   └── pi-ai (14 deps: anthropic, openai, google, bedrock, protobuf, zod, pi-utils...)
├── pi-tui (3 deps: pi-natives, pi-utils, marked)
│   └── pi-natives (75 MB Rust binaries, 版本落后!)
├── pi-utils (3 deps: beautiful-mermaid, winston, winston-daily-rotate-file)
├── pi-natives (75.4 MB, 版本 13.3.7)
├── omp-stats (7 deps: react, react-dom, chart.js, lucide-react, tailwindcss, date-fns...)
└── 其他: @agentclientprotocol/sdk, @mozilla/readability, @xterm/headless,
         fflate, handlebars, linkedom, markit-ai, **puppeteer**, zod
```

**关键差异解读：**

- **puppeteer**（oh-my-pi 独有）：浏览器自动化，下载 Chromium 二进制 ~200 MB
- **omp-stats**：本地监控面板（React + Chart.js），Telegram Bot 用不上，但作为依赖会被安装
- **react / react-dom / tailwindcss**（通过 omp-stats 间接引入）：前端框架，对后端 Bot 无用
- **@agentclientprotocol/sdk**：多 Agent 通信协议，单用户场景不需要

### 2.5 LLM Provider 支持

| Provider | mariozechner | oh-my-pi |
|---|---|---|
| Anthropic Claude | ✅ | ✅ |
| OpenAI (GPT/Codex) | ✅ | ✅ |
| Google Gemini / Vertex | ✅ | ✅ |
| AWS Bedrock | ✅ | ✅ |
| Mistral | ✅ | ✅ |
| Groq / Cerebras / xAI | ✅ | ✅ (可能) |
| OpenRouter | ✅ | ✅ |
| Cursor | ❌ | ✅ |
| Ollama / LM Studio / llama.cpp / vLLM | ✅ (openai-compat) | ✅ (原生) |
| ZAI (智谱) | ✅ | ✅ |

两者 LLM 覆盖面基本相当。

### 2.6 SDK 导出与 API

**mariozechner 导出（简洁，2 个路径）：**
```
.            → createAgentSession, SessionManager, ModelRegistry, AuthStorage, tools, ...
./hooks      → Hook 系统
```

**oh-my-pi 导出（70+ 子路径）：**
```
.                      → 同上 + 更多
./async                → 异步辅助
./capability           → 能力检测
./commit, ./commit/*   → Git commit agent
./commands/*           → 内置命令
./extensibility/*      → 扩展系统
./lsp, ./lsp/clients   → LSP 集成
./mcp, ./mcp/transports→ MCP 协议
./memories             → 记忆系统
./plan-mode/*          → Plan mode
./stt                  → 语音转文字
./web/*                → Web 抓取/搜索
./tools                → 工具定义
...（还有 50+ 子路径）
```

**解读：** oh-my-pi API 远比原版丰富，但 70+ 子路径中大部分对 Telegram Bot 项目无用。只用得到 `.` 和 `./tools`。

### 2.7 oh-my-pi 独有功能评估

| 功能 | 说明 | 对本项目价值 |
|---|---|---|
| 浏览器自动化 (puppeteer) | 内置 Chromium 控制 | 低 — Phase 4 可按需自建 |
| LSP 集成 | 代码语言服务器 | 无 — Bot 场景不适用 |
| MCP 协议 | Agent 通信协议 | 低 — 单用户不需要 |
| Commit Agent | 生成 commit message | 低 — 可自建 |
| 语音转文字 (STT) | TTS/STT 支持 | 中 — 可后续考虑 |
| 记忆系统 (memories) | 内置记忆管理 | 中 — Phase 4 需要 |
| Web 抓取 (readability) | 网页内容提取 | 低 — 可后续按需加 |
| Plan Mode | 规划模式 | 低 — 可自建 |
| omp-stats | 本地监控面板 | 无 — CLI 面板，Bot 用不上 |
| PTY | 伪终端 | 低 — bash tool 够用 |

### 2.8 稳定性对比

| 维度 | mariozechner | oh-my-pi |
|---|---|---|
| 开发周期 | ~5 个月 | ~3 个月 |
| 发布频率 | ~37 版/月 | **~90 版/月** |
| 版本号含义 | 语义化，0.x 表明仍在演进 | 跳跃式（0.x→13.x），内部大重构 |
| 依赖版本同步 | 4 个包版本一致（0.64.0） | pi-natives 停在 13.3.7，**不同步** |
| 文档 | README 22KB + docs 20+ 文件 | 主要继承原版，增量文档少 |

oh-my-pi 发布频率极高（~90 版/月），API 变化非常频繁。pi-natives 版本不同步是一个明确的维护风险信号。

### 2.9 结论：选 @mariozechner/pi-coding-agent

**理由：**

1. **Node.js 兼容**：编译为 JS，标准 Node.js 20+ 运行，不需要 Bun。方案已选 Node.js + npm
2. **体积差距**：~15 MB vs ~300 MB+，对"轻量、易部署"目标影响显著
3. **依赖干净**：4 个包版本同步，无 React/Chart.js/puppeteer 等无关依赖
4. **API 稳定性**：发布频率低，API 变化可控，导出简洁，学习成本低
5. **渐进式**：Phase 4 如需浏览器/记忆等高级功能，可在工具层自建

**交换代价**：放弃 oh-my-pi 内置的记忆系统、浏览器自动化、MCP 协议等。这些在 Phase 1-3 都不需要，Phase 4 可按需实现。

## 3. 架构

```
┌─────────────────────────────────────────────┐
│                  用户                        │
│            (Telegram App)                    │
└──────────────┬──────────────────────────────┘
               │ Telegram Bot API
               ▼
┌─────────────────────────────────────────────┐
│           Telegram 接入层 (grammy)           │
│  ┌─────────────┐  ┌───────────────────────┐ │
│  │ 消息接收     │  │ 流式回复/编辑消息      │ │
│  └──────┬──────┘  └───────────▲───────────┘ │
└─────────┼─────────────────────┼─────────────┘
          │                     │
          ▼                     │
┌─────────────────────────────────────────────┐
│              会话管理层                       │
│  ┌─────────────┐  ┌───────────────────────┐ │
│  │ 会话路由     │  │ Agent 实例池           │ │
│  │ (chatId→Agent)│ │ (per-chat isolation)  │ │
│  └─────────────┘  └───────────────────────┘ │
└─────────┬───────────────────────▲───────────┘
          │ prompt()             │ subscribe()
          ▼                     │
┌─────────────────────────────────────────────┐
│              Pi Agent SDK                    │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐ │
│  │ pi-ai    │  │ Agent    │  │ Tools      │ │
│  │ (LLM层)  │  │ Runtime  │  │ (可配置)   │ │
│  │          │  │ (事件流) │  │            │ │
│  └──────────┘  └──────────┘  └───────────┘ │
└─────────────────────────────────────────────┘
```

## 4. 核心模块

### 4.1 LLM 调用层（pi-ai）

**职责：** 统一 LLM API，支持多 provider 切换

**支持的 Provider：**
- Anthropic (Claude)
- OpenAI (GPT)
- Google (Gemini)
- OpenAI 兼容端点（GLM、DeepSeek 等）
- 本地模型（Ollama / LM Studio）

**GLM 接入示例：**
```json
{
  "providers": {
    "zai": {
      "type": "openai-compatible",
      "baseUrl": "https://open.bigmodel.cn/api/paas/v4",
      "apiKey": "${ZAI_API_KEY}"
    }
  }
}
```

### 4.2 Agent 运行时（pi-agent）

**职责：** 管理对话状态、工具调用循环、事件流

**核心 API：**
```typescript
agent.prompt(text)           // 发送消息
agent.subscribe(callback)    // 监听事件流
agent.setModel(model)        // 动态切换模型
agent.abort()                // 取消当前操作
agent.steer(message)         // 中途干预
agent.reset()                // 重置会话
```

**事件流：**
```
agent_start → turn_start → message_start → message_update*(流式) 
→ message_end → [tool_execution_start → tool_execution_end]* 
→ turn_end → agent_end
```

### 4.3 Telegram 接入层（grammy）

**职责：** 接收消息、流式回复、命令处理

**关键功能：**
- 消息接收 → 转发给 Agent
- 流式输出 → 编辑 Telegram 消息（节流 1-2s）
- 图片/文件 → 下载后作为 attachment 传给 Agent
- Bot 命令处理

### 4.4 工具配置（可切换）

三种工具模式，可通过命令切换：

| 模式 | 包含工具 | 适用场景 |
|---|---|---|
| `coding` | read + write + edit + bash + glob + grep | 代码开发 |
| `readonly` | read + glob + grep | 代码阅读/分析 |
| `custom` | 用户自定义工具列表 | 特定需求 |

```typescript
const toolPresets = {
  coding: [readTool, writeTool, editTool, bashTool, globTool, grepTool],
  readonly: [readTool, globTool, grepTool],
  custom: (tools: Tool[]) => tools,
};
```

切换命令：`/tools coding` / `/tools readonly` / `/tools custom`

### 4.5 会话管理

**策略：** 单用户场景，简化为每个 chatId 对应一个 Agent 实例

```typescript
const sessions = new Map<string, Agent>();

function getOrCreateAgent(chatId: string): Agent {
  if (!sessions.has(chatId)) {
    sessions.set(chatId, createAgent());
  }
  return sessions.get(chatId)!;
}
```

**会话持久化：** Agent 状态可序列化为 JSON，保存到本地文件

## 5. 数据流

### 5.1 用户发消息（流式）

```
1. Telegram → grammy callback → 提取 text + chatId
2. 查找/创建 Agent 实例
3. 发送 "思考中..." 占位消息
4. agent.prompt(text) + subscribe 事件流
5. message_update 事件 → 累积文本 → 节流 1-2s → 编辑 Telegram 消息
6. tool_execution_start → 追加 "⏳ 正在执行: {toolName}({args})" 到消息
7. tool_execution_end → 更新状态
8. agent_end → 发送最终完整消息
```

### 5.2 长消息分段

```
Telegram 消息限制 4096 字符
→ 超过时自动拆分为多条消息
→ 拆分点优先选择段落/换行处
```

### 5.3 工具调用流程

```
用户: "读一下 config.json 的内容"
  → Agent 调用 read tool
  → tool_execution_start: { toolName: "read", args: { path: "config.json" } }
  → Telegram 编辑: "⏳ 正在执行: read(config.json)"
  → tool_execution_end: { result: "..." }
  → Agent 继续推理，流式输出回复
  → Telegram 显示: "这个配置文件的内容是..."
```

## 6. 安全设计

### 6.1 单用户安全模型

作为单用户自部署项目，安全边界较简单，但仍需考虑：

| 风险 | 缓解措施 |
|---|---|
| bash 工具执行任意命令 | readonly 模式下禁用 bash；可配置命令白名单 |
| Telegram token 泄露 | .env 文件管理，不提交到 git |
| API key 泄露 | 同上，.env 管理 |
| 工作目录越界 | Agent 工作目录限制在指定目录内 |

### 6.2 Bash 工具安全

```typescript
const bashSafety = {
  allowedDirs: ["/home/user/projects"],
  blockedCommands: ["rm -rf /", "sudo"],
  timeout: 30000,
};
```

### 6.3 权限分级

```
/tools readonly  → 最安全，只能读文件
/tools coding    → 可读写文件 + 执行命令
/tools custom    → 用户自定义
```

## 7. 技术选型

| 组件 | 选择 | 理由 |
|---|---|---|
| 语言 | TypeScript | Pi SDK 原生语言 |
| 运行时 | Node.js 22+ | 原生 TypeScript 支持 |
| Telegram 框架 | grammy | 轻量、TypeScript 原生、流式编辑支持好 |
| Agent SDK | @mariozechner/pi-coding-agent | 核心引擎（见 2.9） |
| LLM 层 | pi-ai (SDK 内置) | 统一 API |
| 包管理 | npm | 用户更熟悉 |
| 进程管理 | pm2 (Linux) / 直接运行 (Windows) | 本地部署 |

## 8. 目录结构

```
pi-telegram-agent/
├── src/
│   ├── index.ts           # 入口，启动 bot
│   ├── bot.ts             # grammy bot 配置 + 消息处理
│   ├── agent-factory.ts   # Agent 实例创建 + 工具注册
│   ├── session-manager.ts # 会话管理（chatId → Agent）
│   ├── stream-handler.ts  # 流式输出 → Telegram 消息编辑
│   ├── tools/
│   │   ├── index.ts       # 工具预设定义
│   │   ├── coding.ts      # coding 模式工具集
│   │   └── readonly.ts    # readonly 模式工具集
│   └── config.ts          # 配置加载（API key、model 等）
├── config/
│   └── models.json        # LLM provider 配置
├── data/
│   └── sessions/          # 会话持久化存储
├── .env.example           # 环境变量模板
├── .gitignore
├── package.json
├── tsconfig.json
└── README.md              # 部署说明
```

## 9. 功能分阶段

### Phase 1: MVP（约 3-4 小时）

- [ ] 项目初始化（package.json、tsconfig、依赖安装）
- [ ] Telegram Bot 消息收发（grammy 基础）
- [ ] Pi Agent 实例创建 + 对话
- [ ] 流式输出（编辑 Telegram 消息，节流 1-2s）
- [ ] 基础工具（read/write/edit/bash）
- [ ] GLM 模型接入
- [ ] 错误处理 + 超时

### Phase 2: 工具管理 + 命令（+2 小时）

- [ ] 工具模式切换（/tools coding/readonly/custom）
- [ ] Bot 命令：/new（新会话）、/model（切换模型）、/tools（切换工具模式）
- [ ] 工具执行状态实时显示

### Phase 3: 持久化（+1-2 小时）

- [ ] 会话序列化/反序列化
- [ ] 程序重启后恢复会话
- [ ] 会话过期清理

### Phase 4: 增强（按需）

- [ ] 图片/文件支持
- [ ] 记忆系统（MEMORY.md 模式）
- [ ] 自定义 skills 加载
- [ ] 多 Agent 协调（subagent）
- [ ] Heartbeat 机制

## 10. 风险与限制

| 风险 | 影响 | 缓解 |
|---|---|---|
| Telegram 消息长度限制（4096 字符） | 长回复被截断 | 自动分段发送 |
| Telegram 编辑消息频率限制 | 流式输出不流畅 | 节流到 1-2s 间隔 |
| 工具执行耗时过长 | 用户等太久 | 设 30s 超时 + 进度提示 |
| Agent 内存占用 | 多会话 OOM | 单用户场景问题不大；空闲回收 |
| Pi SDK API 变化 | 接口不兼容 | 锁版本 + 关注 changelog |
| 单用户 Telegram 限制 | 无法多人使用 | 明确项目定位；未来可扩展 |

## 11. 待讨论

- [x] **SDK 选择**：已确定选 @mariozechner/pi-coding-agent（见 2.9）
- [ ] **工作目录**：Agent 默认操作哪个目录？用户可配置？
- [ ] **bash 超时**：30s 是否合适？是否需要可配置？
- [ ] **会话数量上限**：单用户场景下是否需要限制？

## 12. 下一步

1. ~~确认 SDK 选择~~ ✅ 已确定
2. 讨论待讨论事项（工作目录、bash 超时等）
3. 创建项目目录 + 初始化
4. Phase 1 开发

## 13. 方案评估

> 对上述方案各部分的完整审查，按模块逐项指出优点、问题和改进建议。

### 13.1 SDK 选型（第 2 节）— 充分，结论合理

**优点：**
- 对比维度覆盖全面：运行时、包体、依赖链、API 面、稳定性，数据均有来源
- 结论与项目定位（轻量、单用户、Node.js）一致，权衡清晰

**需补充：**
- SDK API 的实际验证缺失。`createAgentSession()` 的签名、参数、返回类型、事件名都是从 README/docs 推断的，尚未跑过代码。建议 Phase 1 第一步先写一个最小 SDK demo（10 行），在本地验证 `createAgentSession` + `subscribe` 是否如文档所述能工作，确认事件名、流式回调格式等
- 没有提到 pi-coding-agent 是 CLI-first 设计，SDK 模式（headless/非 TUI）是否完整暴露所有能力还不确定。例如 `agent-session.js`（114 KB）是主要逻辑，但它内部是否硬依赖 TUI 渲染、stdin 等终端环境需要验证
- 版本锁定策略只写了"锁版本"，但没说锁到什么精度。建议 `package.json` 里用精确版本（`"0.64.0"` 而非 `"^0.64.0"`），因为 0.x 版本任何 minor bump 都可能有 breaking change

### 13.2 架构（第 3 节）— 方向正确，但层次描述偏抽象

**优点：**
- 三层架构（Telegram 接入层 → 会话管理层 → Pi SDK）划分清晰
- ASCII 图直观

**问题：**
- **缺少错误传播路径。** 架构图只画了正向数据流（prompt → subscribe），没有画异常流向。LLM 调用失败、工具执行超时、Telegram API 限流这三类错误分别从哪一层捕获、怎么向上通知用户？需要画出来
- **"会话管理层"和"Agent 实例池"过度设计名称。** 实际上单用户场景 + Map<chatId, Agent> 就够了，不需要"池"的概念（没有复用/回收/上限逻辑）。命名应准确反映实现复杂度
- **缺少配置加载的位置。** `.env` 加载、`models.json` 加载、工具预设加载这些初始化逻辑没有在架构中体现。建议在架构图底部加一个 Config 层

### 13.3 核心 API 假设（第 4.2 节）— 高风险，需实际验证

**问题：**

第 4.2 节列出的 API（`agent.prompt()`, `agent.subscribe()`, `agent.setModel()`, `agent.abort()`, `agent.steer()`, `agent.reset()`）和事件流（`agent_start → turn_start → ...`）是文档中的概念，但：

1. **SDK 实际导出的是 `createAgentSession()`**，不是一个简单的 `Agent` 对象。`SessionManager` 和 `createAgentSession` 的区别、它们各自的 API 都不清楚。方案用了一个简化的 `agent` 对象来描述，但实际接口可能完全不同
2. **事件名未验证。** `agent_start`, `message_update`, `tool_execution_start` 等事件名来自推测。Pi SDK 的实际事件结构可能是 `{ type: "message", delta: "..." }` 这样的格式，而非独立事件名
3. **流式回调的数据格式未知。** `message_update` 携带的是完整文本还是增量 delta？这直接影响 stream-handler 的实现方式

**建议：** 在 Phase 1 开始前，用以下方法验证实际 API：
```bash
# 方法 1：直接读 SDK 源码
npm pack @mariozechner/pi-coding-agent@0.64.0 && tar xzf *.tgz
# 查看 dist/index.d.ts 和 dist/core/agent-session.d.ts

# 方法 2：看 examples/sdk/ 目录（SDK 自带 12 个示例）
# 01-minimal 到 12-full-control，覆盖了各种用法
```

### 13.4 Telegram 接入层（第 4.3 节）— 关键细节不足

**缺少的关键设计：**

1. **Markdown 渲染兼容性。** Pi Agent 输出标准 Markdown，但 Telegram 的 MarkdownV2 语法与标准 Markdown 差异很大（需要转义 `.`, `-`, `(`, `)` 等大量字符）。直接把 agent 输出送给 Telegram 会导致 parse error，消息发送失败。需要一个 `markdownToTelegramV2()` 转换层，或者退而使用 HTML parse mode
2. **并发消息处理。** 用户在 agent 还在回复时发了第二条消息怎么办？目前方案没提到。选项：(a) 排队等当前 turn 结束 (b) abort 当前 turn 处理新消息 (c) 拒绝并提示"请等待当前回复完成"。建议 Phase 1 先用 (c)，最简单
3. **grammy polling vs webhook。** 方案没有说明使用 long polling 还是 webhook。单用户本地部署场景下 long polling 最简单（不需要公网 IP/域名），但应该明确写出来
4. **Bot 命令注册。** grammy 的 `bot.command()` 需要注册，但 Phase 1 只列了消息收发，命令在 Phase 2。建议 Phase 1 就注册 `/start` 命令，作为 Bot 的入口

### 13.5 流式输出（第 5.1 节）— 思路对，细节需修正

**问题：**

1. **"思考中..." 占位消息 + 后续编辑"策略有隐患。** Telegram `editMessageText` 在内容没有变化时会报 `Bad Request: message is not modified` 错误。流式输出中如果两次节流窗口内文本恰好没变化（比如 agent 在做工具调用），就会触发此错误。需要在编辑前检查内容是否真的变了
2. **节流时间 1-2s 需要更精确。** Telegram Bot API 对同一条消息的编辑有约 1 次/秒的软限制（超过会 429 Too Many Requests）。建议硬编码为 **1.5s**，并加 429 响应的 retry-after 处理
3. **步骤 8 "发送最终完整消息"不合理。** 如果流式编辑已经把完整内容写到了消息里，agent_end 时再"发送最终完整消息"就是重复的。应该改为：agent_end 时做一次最终编辑（确保最后一个 chunk 没被节流丢掉），而不是新发一条消息
4. **长消息分段（5.2）过于简化。** 分段发送意味着流式编辑只能对最后一条消息进行。如果第一条消息已发出、第二条正在流式编辑中，用户体验其实还行。但需要考虑：分段点如何在流式过程中判断？建议简化为：流式阶段只编辑一条消息，agent_end 后如果总长度超过 4096 才做分段重发

### 13.6 安全设计（第 6 节）— 过于乐观

**问题：**

1. **`blockedCommands: ["rm -rf /", "sudo"]` 是无效防护。** 字符串黑名单极易绕过（`sudo` → `su -c`，`rm -rf /` → `find / -delete`），给人虚假安全感。对单用户场景，真正的安全模型应该是：**用户信任自己，风险来自 LLM 的幻觉行为。** 正确做法是给 bash 工具加确认机制（高危命令前向 Telegram 发确认消息），而非黑名单
2. **工作目录限制（6.1）没有实现方案。** 只说了"限制在指定目录内"，但 Pi SDK 的 bash 工具是否支持 `cwd` 约束？如果不支持，需要自己 wrap bash 工具或用 chroot/namespace
3. **缺少 Telegram chatId 鉴权。** 单用户项目必须限制只有指定的 Telegram userId 能使用 Bot，否则任何人加了 Bot 都能用。这是最基本的安全措施，方案完全没提到。建议加一个 `ALLOWED_USER_ID` 环境变量，在消息处理最外层做检查

### 13.7 会话管理（第 4.5 节）— 可用，但序列化假设未验证

**问题：**

1. **"Agent 状态可序列化为 JSON"未验证。** Pi SDK 的 `SessionManager` 用 JSONL 格式存储会话，但这是 SDK 内部行为。通过 `createAgentSession()` 创建的实例是否暴露了 serialize/deserialize 方法？如果没有，Phase 3 的"程序重启后恢复会话"就需要完全不同的实现方案（比如直接利用 SDK 的 SessionManager）
2. **chatId 类型。** Telegram 的 chatId 是 `number`（不是 `string`），`Map<string, Agent>` 的键类型有误

### 13.8 工具配置（第 4.4 节）— 合理，小问题

- 代码示例中的 `readTool`, `writeTool` 等变量名是假设的，实际 SDK 导出的工具注册方式可能不同（可能是字符串名称而非对象引用）
- `custom` 模式的 UX 设计缺失：用户怎么通过 Telegram 消息指定自定义工具列表？JSON 字符串？逗号分隔名称？

### 13.9 目录结构（第 8 节）— 合理，建议微调

- `tools/coding.ts` 和 `tools/readonly.ts` 内容很少（各自只是一个数组），没必要拆成单独文件。合并到 `tools/index.ts` 即可
- `config/models.json` 可能不需要。Pi SDK 有自己的配置目录（`~/.pi/agent/`），模型配置应该走 SDK 自身的机制，避免维护两套配置
- 缺少 `src/utils/` 目录：Markdown 转换、消息分段等工具函数放哪里？

### 13.10 分阶段规划（第 9 节）— 合理，时间估计偏乐观

**Phase 1（3-4 小时）：**
- 如果 SDK API 与假设一致，3-4 小时可行
- 如果 SDK API 有差异（大概率），需要额外 2-3 小时读源码和调试
- **建议拆成 Phase 0（验证 SDK）和 Phase 1（实现 MVP）**，Phase 0 预算 1-2 小时

**Phase 2（+2 小时）：**
- 时间估计合理

**Phase 3（+1-2 小时）：**
- 取决于 SDK 是否原生支持 session 持久化。如果 `SessionManager` 已经处理了 JSONL 存储，Phase 3 可能只需要很少代码。如果需要自建，2 小时不够

**Phase 4：**
- "图片/文件支持"应提前到 Phase 2 或 Phase 3。Telegram 用户发图片是非常自然的行为，不应该拖到 Phase 4
- "Heartbeat 机制"含义不明确，需要解释

### 13.11 风险与限制（第 10 节）— 遗漏了最大的风险

**遗漏风险：**

| 风险 | 影响 | 缓解 |
|---|---|---|
| **SDK headless 模式不完整** | Agent 创建失败或功能受限 | Phase 0 先验证 |
| **Telegram MarkdownV2 不兼容** | 消息发送 parse error | 加转换层或用 HTML |
| **OpenAI-compat 端点兼容问题** | chat/completions 400 错误（已验证 aixj.vip 有此问题） | 确认 pi-ai 用的是 chat/completions 还是 responses API |
| **未鉴权导致 Bot 被滥用** | 任何人可以使用你的 Bot 和 API 额度 | 加 ALLOWED_USER_ID 检查 |
| **Node.js 22 原生 TS 的局限** | `--experimental-strip-types` 不支持 enum、装饰器等 | 如果 SDK 或 grammy 用了这些特性需要加 tsx/tsup |

### 13.12 综合判断

**可行性：高。** 核心思路正确——Pi SDK 作为 agent 引擎，grammy 作为接入层，中间加一层薄的会话管理和流式转发。这个架构是合理的。

**最大不确定性：Pi SDK 的 headless/SDK 模式是否如文档所述完整可用。** 方案中所有 API 调用和事件格式都基于推测，一旦实际接口有差异，大量细节设计需要返工。

**建议的执行顺序调整：**

```
Phase 0: SDK 验证（1-2h）
  ├── npm install @mariozechner/pi-coding-agent@0.64.0
  ├── 读 dist/index.d.ts，确认实际导出
  ├── 跑 examples/sdk/01-minimal，确认 headless 可用
  ├── 确认事件名、流式回调格式、工具注册方式
  └── 确认 pi-ai openai-compat provider 走的是 chat/completions 还是 responses

Phase 1: MVP（3-4h，基于 Phase 0 结论）
  ├── 项目初始化
  ├── Telegram Bot + userId 鉴权
  ├── Agent 创建（基于 Phase 0 验证的实际 API）
  ├── 流式输出（含 Markdown 转换、节流、429 处理）
  └── 基础工具 + 错误处理

Phase 2: 命令 + 图片（+2-3h）
  ├── Bot 命令（/new, /model, /tools）
  ├── 图片/文件消息支持（提前）
  └── 工具执行状态显示

Phase 3: 持久化（+1-2h）
Phase 4: 增强（按需）
```

### 13.13 文档质量

- 结构清晰，分节合理，可读性好
- SDK 选型部分数据详实有说服力
- 代码示例适量，不过多也不过少
- 主要问题：API 层面的假设成分过重，需要尽快用实际验证替代

---

*评估完成。核心建议：增加 Phase 0（SDK 实际验证），补充 Telegram userId 鉴权、MarkdownV2 转换、并发消息处理三个关键缺失设计。*
