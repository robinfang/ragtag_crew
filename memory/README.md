# Memory Files

这个目录放长期记忆的正文文件。

建议：

- `preferences.md`：个人偏好
- `project-decisions.md`：项目关键决策
- `YYYY-MM-DD.md`：阶段性工作记录
- `inbox.md`：运行中快速追加的待整理笔记

推荐流程：

- 先用 `/memory add <note>` 追加到 `inbox.md`
- 再用 `/memory promote` 或 `/memory promote preferences.md` 做手动整理
- 用 `/memory show <name>` 检查整理结果
